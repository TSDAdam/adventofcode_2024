from copy import deepcopy
with open('./04.test') as file:
    data = [line.strip() for line in file]
word = 'XMAS'

def checkForWord(i,)

xs = [-1, 0, 1]
ys = [-1, 0, 1]
for xx, row in enumerate(data):
    for yy, c in enumerate(row):
        if c == 'X':
            
